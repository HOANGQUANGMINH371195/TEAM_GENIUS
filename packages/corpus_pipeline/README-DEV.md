# Corpus pipeline package — developer guide

This is a compatibility package for offline callers. The authoritative code is
`database/pipeline/data_pipeline`; run its tests and release commands from the
repository root. Do not add this package to the API or web deployment image.
