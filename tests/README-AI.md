# Tests — AI contract

Every behavior change needs a focused regression test. Keep external providers,
databases and credentials mocked or contract-tested; never make the suite
depend on a live Render, Vercel, Firebase, Neo4j, Qdrant or OpenAI account.
