# Operations — AI contract

Treat deployment and restore as fail-closed workflows. Do not invent service
IDs, domains, credentials or successful external writes. Keep platform tokens
out of source, command arguments and reports; report missing external gates
explicitly. Update the relevant runbook whenever an environment or release
contract changes.
