# Firebase — AI contract

Firebase ID-token verification is the identity boundary. UIDs are opaque
strings, not UUIDs. Authorization decisions use the verified UID and server
role record; client-provided owner IDs are never trusted.
